# key/file

本地文件实现由 `key.NewKey` 内部使用，业务不需要直接初始化该子包。

当 `key.yaml` 未配置，或 `type` 为空/未知时，默认读取 `${conf}/root.key`。文件内容可以是原始 32 字节，
也可以是 Base64 编码的根密钥；生产环境请将文件权限限制为 `0400` 或 `0600`。

Provider 初始化时会使用系统随机源创建缺失的根密钥文件并设置 `0600` 权限；已有文件直接复用，不会覆盖。

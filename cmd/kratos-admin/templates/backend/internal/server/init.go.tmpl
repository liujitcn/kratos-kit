package server

import (
	business "__MODULE_PATH__/internal/server/__FRONTEND_MODULE__"
	"github.com/google/wire"
)

// ProviderSet 汇总项目的 server 层依赖。
var ProviderSet = wire.NewSet(business.ProviderSet)

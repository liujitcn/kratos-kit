// Package server 提供当前项目的传输服务注册入口。
package server

import (
	"github.com/google/wire"

	"__MODULE_PATH__/internal/server/middleware"
)

// ProviderSet 汇总服务模块与中间件依赖注入提供者。
var ProviderSet = wire.NewSet(
	middleware.ProviderSet,
	NewModule,
)

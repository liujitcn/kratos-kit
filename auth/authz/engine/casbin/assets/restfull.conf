[request_definition]
r = tenant, sub, obj, act

[policy_definition]
p = tenant, sub, obj, act

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = r.tenant == p.tenant && r.sub == p.sub && keyMatch(r.obj, p.obj) && regexMatch(r.act, p.act)
